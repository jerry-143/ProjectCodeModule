server:
  port: 1212

spring:
 datasource:
  url: jdbc:mysql://localhost:3306/DatabaseName
  username: root
  password: root
 jpa:
  hibernate: 
   ddl-auto: update
  properties:
   hibernate:
    dialect: org.hibernate.dialect.MySQL5Dialect
logging: 
 level:
  org:
   hibernate:
    SQL: debug
    type:
     descriptor:
      sql: trace
